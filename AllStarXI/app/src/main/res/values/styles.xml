<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/AllStarActionBarTheme</item>
    </style>

    <style name="AllStarActionBarTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:background">@color/allStarDarkGreen</item>
        <item name="android:titleTextStyle">@style/AllStarActionBarTextStyle</item>
    </style>

    <style name="AllStarActionBarTextStyle" parent="@android:Theme.Holo.Light.DarkActionBar">
        <item name="android:textColor">#FFFFFF</item>
    </style>

</resources>
